pipeline {
    agent any

    environment {
        TARGET_URL = "http://netflix-app"
        REPORT_DIR = "security/zap"
        OUTPUT_DIR = "zap-output"
        ARTIFACT_DIR = "/var/jenkins_home/devsecops-artifacts/zap"
    }

    stages {

        stage('Checkout Source') {
            steps {
                checkout scm
            }
        }

        stage('Prepare Report Directory') {
            steps {
                sh '''
                rm -rf ${REPORT_DIR}
                rm -rf ${OUTPUT_DIR}

                mkdir -p ${REPORT_DIR}
                mkdir -p ${OUTPUT_DIR}

                chmod -R 777 ${REPORT_DIR}
                chmod -R 777 ${OUTPUT_DIR}
                '''
            }
        }

        stage('Run ZAP Scan') {
            steps {
                sh '''
                echo "======================================"
                echo "Pulling ZAP Docker Image"
                echo "======================================"

                docker pull ghcr.io/zaproxy/zaproxy:stable

                echo ""
                echo "======================================"
                echo "Running ZAP Baseline Scan"
                echo "======================================"

                docker run --rm \
                  --user root \
                  --network devsecops-network \
                  -v "$WORKSPACE/${OUTPUT_DIR}:/zap/wrk" \
                  ghcr.io/zaproxy/zaproxy:stable \
                  zap-baseline.py \
                  --autooff \
                  -t ${TARGET_URL} \
                  -J zap-report.json \
                  -I

                echo ""
                echo "======================================"
                echo "Files generated by ZAP"
                echo "======================================"

                ls -lah ${OUTPUT_DIR}

                if [ ! -f ${OUTPUT_DIR}/zap-report.json ]; then
                    echo "ERROR : zap-report.json not generated"
                    exit 1
                fi

                cp ${OUTPUT_DIR}/zap-report.json ${REPORT_DIR}/

                echo ""
                echo "Copied report to ${REPORT_DIR}"

                ls -lah ${REPORT_DIR}
                '''
            }
        }

        stage('Verify Report') {
            steps {
                sh '''
                if [ -f ${REPORT_DIR}/zap-report.json ]; then
                    echo ""
                    echo "SUCCESS : zap-report.json generated"
                else
                    echo ""
                    echo "ERROR : zap-report.json missing"
                    exit 1
                fi
                '''
            }
        }

        stage('Publish Report') {
            steps {
                sh '''
                mkdir -p ${ARTIFACT_DIR}

                cp ${REPORT_DIR}/zap-report.json ${ARTIFACT_DIR}/

                echo ""
                echo "Published to ${ARTIFACT_DIR}"

                ls -lah ${ARTIFACT_DIR}
                '''
            }
        }

        stage('Archive Report') {
            steps {
                archiveArtifacts(
                    artifacts: 'security/zap/zap-report.json',
                    fingerprint: true,
                    allowEmptyArchive: false
                )
            }
        }
    }

    post {
        success {
            echo "======================================"
            echo "ZAP Pipeline Successful"
            echo "======================================"
        }

        failure {
            echo "======================================"
            echo "ZAP Pipeline Failed"
            echo "======================================"
        }

        always {
            echo "Pipeline Finished"
        }
    }
}
